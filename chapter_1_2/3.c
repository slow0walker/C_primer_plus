#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>

int main() {
    int age = 20; 
    int days = age * 365;

    printf("Age in years: %d\n", age);
    printf("Age in days: %d\n", days);

    return 0;
}